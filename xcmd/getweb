class XRPN
# Get web page with URL in Alpha into X
  def getweb
    lift
    begin
      @x = open(@a) {|f| f.read }
    rescue
      return "Cannot get the content of \"#{a}\""
    end
  end
end
 
# vim:ft=ruby:
