class XRPN
# Make Page n the current working program Page
  def page (p)
    if p.to_i >= @prg.length
      return "Page number out of range."
    else
      @pg = p.to_i
      @pc = 0
      @rtn = []
    end
  end
end
 
# vim:ft=ruby:
