class XRPN
# Write X as content to file named in Alpha
  def writefile
    begin
      @x = File.write(@a)
      File.write(@a, @x)
    rescue
      return "Cannot write to the file '#{a}'"
    end
  end
end
 
# vim:ft=ruby: