class XRPN
# Read any file named in Alpha into X
  def getfile
    lift
    begin
      @x = File.read(@a).chomp
    rescue
      return "Cannot read the file \"#{a}\""
    end
  end
end
 
# vim:ft=ruby:
