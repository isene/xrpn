class XRPN
# Store Alpha to named register
  def asto (r)
    case r.downcase
    when "x"
      @l = @x
      @x = @a
    when "y"
      @y = @a
    when "z"
      @z = @a
    when "t"
      @t = @a
    when "l"
      @l = @a
    else
      @reg[r] = @a
    end
  end
end
 
# vim:ft=ruby:
