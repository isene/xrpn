class XRPN
# Create data file. No need to supply any file size.
  def crfld
    f = Dir.home + "/.xrpn/data/" + @a + ".da"
    File.write(f, [[f, "DA", 0, 0.0]])
    load_xm(@a, 0.0)
  end
end
 
# vim:ft=ruby: